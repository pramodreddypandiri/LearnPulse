{
  "name": "learnpulse-extension",
  "version": "0.1.0",
  "description": "LearnPulse Chrome Extension — automatic history capture",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  }
}
